# 🔗 URL Shortener Service v2.0
Cервис сокращения ссылок с поддержкой срока действия, ограничения переходов и многопользовательской архитектурой.

## 📁 Структура проекта
```text
src/
├── main/java/com/urlshortener/
│   ├── core/                           
│   │   ├── domain/                     
│   │   │   ├── exceptions/            # Исключения
│   │   │   │   ├── DomainException.java          # Базовое исключение
│   │   │   │   ├── PermissionDeniedException.java # Нет прав доступа
│   │   │   │   ├── UrlNotFoundException.java     # Ссылка не найдена
│   │   │   │   └── ValidationException.java      # Ошибка валидации
│   │   │   ├── models/               
│   │   │   │   ├── Config.java        # Конфигурация приложения
│   │   │   │   ├── ShortenedUrl.java  # Сокращенная ссылка
│   │   │   │   └── User.java          # Пользователь
│   │   │   └── valueobjects/          
│   │   │       ├── ShortCode.java     # Короткий код ссылки
│   │   │       ├── Url.java           # URL (с нормализацией)
│   │   │       └── UserId.java        # Идентификатор пользователя
│   │   ├── ports/                     # Порты (интерфейсы)
│   │   │   ├── input/                 # Входные порты 
│   │   │   │   ├── StatisticsUseCase.java      # Статистика
│   │   │   │   ├── UrlShortenerUseCase.java    # Операции с ссылками
│   │   │   │   └── UserManagementUseCase.java  # Управление пользователями
│   │   │   └── output/                # Выходные порты
│   │   │       ├── ConfigLoader.java     # Загрузка конфигурации
│   │   │       ├── DateTimeProvider.java # Работа со временем
│   │   │       ├── IdGenerator.java      # Генерация ID
│   │   │       ├── UrlRepository.java    # Хранение ссылок
│   │   │       ├── UrlValidator.java     # Валидация URL
│   │   │       └── UserRepository.java   # Хранение пользователей
│   │   └── services/                  # Реализации сервисов
│   │       ├── StatisticsServiceImpl.java    # Реализация статистики
│   │       ├── UrlShortenerServiceImpl.java  # Основная бизнес-логика
│   │       └── UserServiceImpl.java          # Управление пользователями
│   ├── infrastructure/              
│   │   ├── persistence/              # Хранилища данных
│   │   │   ├── FileUrlRepository.java     # JSON-хранилище ссылок
│   │   │   ├── InMemoryUserRepository.java # Хранилище пользователей
│   │   │   └── JsonConfigLoader.java      # Загрузчик конфигурации
│   │   ├── security/                 # Безопасность
│   │   │   └── UserValidator.java    # Валидатор пользователей
│   │   └── utils/                    # Утилиты
│   │       ├── CodeGeneratorImpl.java    # Генератор кодов
│   │       ├── SystemDateTimeProvider.java # Провайдер времени
│   │       └── UrlValidatorImpl.java     # Валидатор URL
│   └── presentation/                 
│       ├── cli/                      # Командная строка
│       │   ├── CommandParser.java    # Парсер команд
│       │   ├── ConsoleColors.java    # Цвета терминала
│       │   ├── StartingUp.java       # Инициализация приложения
│       │   ├── UrlPrinter.java       # Форматированный вывод
│       │   └── URLShortenerCLI.java  # Основной CLI
│       └── Application.java          # Точка входа
── config.json                  # Конфигурационный файл
```

## 📊 Назначение слоев:
1. Core (Ядро) — бизнес-логика, не зависит от внешних систем
2. Infrastructure (Инфраструктура) — реализации портов (JSON-хранилища, валидаторы)
3. Presentation (Представление) — CLI интерфейс для пользователя

## Требования:
- Java 17
- Maven 3.6

## Запуск программы
Для запуска программы необходимо запустить модуль Application, который находится в папке presentation

## 🎮 Как пользоваться сервисом
### Первый запуск:
При первом запуске создается:
- Конфигурационный файл config.json с настройками по умолчанию
- Файл данных data/url_shortener_data.json (будет создан при первой операции)
- Вам предложат создать нового пользователя или использовать существующий

Работа с пользователями:
- Каждый пользователь имеет UUID и короткий ID (первые 8 символов)
- Сохраните ваш UUID для доступа к своим ссылкам в будущем
- Можно переключаться между пользователями командой switch

### Основной цикл работы:
```bash
🔗 Ваш-ID... > [команда] [параметры]
```

## 📋 Поддерживаемые команды
### Создание ссылок:
| Команда | Параметры | Описание |
|-------------|-------------|-------------|
| sh <url> | URL | Создать ссылку с параметрами по умолчанию |
| sh <url> <дата> | URL, Дата (ГГГГ-ММ-ДД) | С датой истечения | 
| sh <url> <переходы> | URL, Число | С ограничением переходов |  
| sh <url> <дата> <переходы> | URL, Часы, Число | С датой и ограничением |
| sh <url> <часы> <переходы> |	URL, Часы, Число | На N часов с ограничением |


Примеры:
```bash
sh https://google.com
sh https://github.com 2026-12-31 23:59
sh https://example.com 50
sh https://yandex.ru 24 100
```

### Управление ссылками:
| Команда | Описание |
|-------------|-------------|
| go <короткая_ссылка> | Перейти по короткой ссылке |
| list | Показать все мои ссылки | 
| edit <ссылка> [новый_url] [новая_дата] | Редактировать ссылку | 
| delete <короткая_ссылка> | Удалить ссылку (с подтверждением) | 

Примеры:
```bash
go click.by/abc123
info click.by/xyz789
edit click.by/abc123 https://new-url.com
edit click.by/abc123 2025-01-01
delete click.by/abc123
```

### Управление пользователями:
| Команда | Описание |
|-------------|-------------|
| newuser | Создать нового пользователя |
| switch <uuid или короткий-id> | Переключиться на пользователя | 
| whoami | Показать текущего пользователя | 


Примеры:
```bash
newuser
switch 3f5a8c2b
switch 3f5a8c2b-7e4d-4a9c-8b1d-2f6e8c5a1b3d
whoami
```

### Статистика и информация:
Команда	Описание
stats	Показать глобальную и пользовательскую статистику
config	Показать конфигурацию приложения
help	Показать справку по командам
exit	Выйти из приложения
| Команда | Описание |
|-------------|-------------|
| stats | Показать глобальную и пользовательскую статистику |
| config | Показать конфигурацию приложения | 
| help | Показать справку по командам | 
| exit | Выйти из приложения |

## ⚙️ Конфигурация:
Файл config.json (создается автоматически):
```json
{
  "baseUrl": "click.by/",
  "defaultTTLHours": 24,
  "defaultMaxClicks": 100,
  "shortCodeLength": 6,
  "storageFile": "data/url_shortener_data.json",
  "cleanupIntervalMinutes": 60,
  "enableAutoRedirect": true,
  "dateTimeFormat": "yyyy-MM-dd HH:mm",
  "maxTTLDays": 365
}
```

### Параметры конфигурации:
| Команда | По умолчанию | Описание |
|-------------|-------------|-------------|
| baseUrl | click.by/ | Базовый URL для коротких ссылок |
| defaultTTLHours | 24 | Срок действия по умолчанию (часы) | 
| defaultMaxClicks | 100 | Макс. переходов по умолчанию |  
| shortCodeLength | 6 | Длина короткого кода (4-10) |
| storageFile |	data/...json | Файл для хранения данных |
| cleanupIntervalMinutes | 60 | Интервал очистки просроченных ссылок |  
| enableAutoRedirect | true | Автоматически открывать ссылки в браузере |
| maxTTLDays |	365 | Максимальный срок действия (дней) |

## 🧪 Тестирован
### Написанные тесты
Проект включает 4 юнит-теста, проверяющих ключевые функции системы:

**1. UniqueShortCodesTest - Проверка уникальности коротких кодов**
**Что тестируется:** Убедиться, что одна и та же ссылка, сокращенная разными пользователями, генерирует уникальные короткие ссылки.
**Тестовые сценарии:**
- Создание ссылки на https://example.com для пользователя A → код ABC123
- Создание той же ссылки для пользователя B → код XYZ789
- Проверка, что коды разные: ABC123 ≠ XYZ789
**Ожидаемый результат**: Каждый пользователь получает уникальный код для одной и той же ссылки.

**2. ClickLimitTest - Проверка блокировки при исчерпании лимита переходов**
**Что тестируется:** Проверка, что при исчерпании лимита переходов переход по ссылке блокируется.
**Тестовые сценарии:**
- Создание ссылки с лимитом в 3 перехода
- Успешное выполнение первых 3 переходов
- Попытка 4-го перехода → должно выбрасываться исключение
- Проверка содержания сообщения об ошибке
**Ожидаемый результат**: После исчерпания лимита система блокирует доступ с понятным сообщением.

**3. ExpirationCleanupTest - Тестирование удаления просроченных ссылок**
**Что тестируется:** Проверка удаления ссылок по истечении срока жизни с различными временными интервалами.
**Тестовые сценарии:**
- Создание ссылки, истекшей час назад → должна быть недоступна
- Создание ссылки, истекшей сутки назад → должна быть недоступна
- Создание активной ссылки (истекает через час) → должна быть доступна
- Проверка методов canBeAccessed() и isExpired()
- Удаление истекших ссылок из репозитория
**Ожидаемый результат**: Система корректно определяет и удаляет «протухшие» ссылки.

**4. UserNotificationTest - Проверка уведомлений пользователям**
**Что тестируется:** Убедиться, что пользователи получают понятные уведомления о недоступности ссылок.
**Тестовые сценарии:**
- Исчерпан лимит переходов → сообщение "Лимит переходов исчерпан"
- Истек срок действия → сообщение "Срок действия ссылки истек"
- Ссылка заблокирована → сообщение "Ссылка заблокирована"
**Ожидаемый результат**: Для каждой причины недоступности пользователь получает соответствующее уведомление.

### Технологии тестирования
- JUnit 5 - основной фреймворк для юнит-тестов
- Mockito - для создания моков зависимостей
- JUnit Pioneer - для работы с временными директориями (@TempDir)
