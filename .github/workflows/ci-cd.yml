name: Java CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    - cron: '0 2 * * 1'  # Каждый понедельник в 2:00 AM

env:
  JAVA_VERSION: '17'
  MAVEN_VERSION: '3.9.6'
  PROJECT_NAME: 'url-shortener'
  REPORT_DIR: 'reports'

jobs:
  # Job 1: Сборка проекта
  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Полная история для анализа
    
    - name: ⚙️ Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: ${{ env.JAVA_VERSION }}
        cache: 'maven'
    
    - name: 🔍 Validate pom.xml
      run: |
        echo "🔍 Проверка валидности pom.xml..."
        mvn help:effective-pom -q
        echo "✅ pom.xml валиден"
    
    - name: 📦 Build with Maven
      run: |
        echo "🏗️ Начинаем сборку проекта..."
        mvn clean compile -DskipTests
        echo "✅ Сборка завершена успешно"
    
    - name: 📊 Generate build report
      run: |
        echo "📊 Генерация отчета о сборке..."
        mkdir -p ${{ env.REPORT_DIR }}
        mvn dependency:tree -DoutputFile=${{ env.REPORT_DIR }}/dependency-tree.txt
        echo "✅ Отчет зависимостей сгенерирован"
    
    - name: 💾 Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-artifacts
        path: |
          target/classes/
          ${{ env.REPORT_DIR }}/dependency-tree.txt
        retention-days: 7

  # Job 2: Тестирование
  test:
    name: 🧪 Test
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
    
    - name: ⚙️ Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: ${{ env.JAVA_VERSION }}
        cache: 'maven'
    
    - name: 🧪 Run unit tests
      run: |
        echo "🧪 Запуск юнит-тестов..."
        mvn test
        
        # Проверяем результаты тестов
        if [ -f "target/surefire-reports/TEST-com.urlshortener.test.UniqueShortCodesTest.xml" ]; then
          echo "✅ UniqueShortCodesTest выполнен"
        fi
        if [ -f "target/surefire-reports/TEST-com.urlshortener.test.ClickLimitTest.xml" ]; then
          echo "✅ ClickLimitTest выполнен"
        fi
        if [ -f "target/surefire-reports/TEST-com.urlshortener.test.ExpirationCleanupTest.xml" ]; then
          echo "✅ ExpirationCleanupTest выполнен"
        fi
        if [ -f "target/surefire-reports/TEST-com.urlshortener.test.UserNotificationTest.xml" ]; then
          echo "✅ UserNotificationTest выполнен"
        fi

  # Job 3: Линтинг и анализ кода
  lint-and-analyze:
    name: 🔍 Lint & Analyze
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
    
    - name: ⚙️ Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: ${{ env.JAVA_VERSION }}
        cache: 'maven'
    
    - name: 📋 Code Quality Checks
      run: |
        echo "🔍 Проверка качества кода..."
        mkdir -p ${{ env.REPORT_DIR }}
        
        # Проверяем наличие pom.xml
        if [ -f "pom.xml" ]; then
          echo "📋 Используем Maven для проверок..."
          
          # Проверка Checkstyle если настроен
          if grep -q "checkstyle" pom.xml; then
            echo "📝 Запуск Checkstyle..."
            mvn checkstyle:check || echo "⚠️  Checkstyle нашел проблемы"
          fi
          
          # Проверка PMD если настроен
          if grep -q "pmd" pom.xml; then
            echo "📝 Запуск PMD..."
            mvn pmd:check || echo "⚠️  PMD нашел проблемы"
          fi
        else
          echo "ℹ️  pom.xml не найден, выполняем базовые проверки..."
        fi
        
        # Базовые проверки для любого проекта
        echo "📏 Проверка стиля кода..."
        
        # Создаем отчет
        echo "# Отчет анализа кода" > ${{ env.REPORT_DIR }}/code-analysis.md
        echo "Дата: $(date)" >> ${{ env.REPORT_DIR }}/code-analysis.md
        echo "" >> ${{ env.REPORT_DIR }}/code-analysis.md
        
        # 1. Проверка табов
        echo "## 1. Проверка использования табов" >> ${{ env.REPORT_DIR }}/code-analysis.md
        TAB_FILES=$(grep -l $'\t' $(find . -name "*.java") 2>/dev/null | wc -l || echo "0")
        if [ "$TAB_FILES" -gt 0 ]; then
          echo "❌ Найдены файлы с табами: $TAB_FILES" >> ${{ env.REPORT_DIR }}/code-analysis.md
          grep -l $'\t' $(find . -name "*.java") 2>/dev/null | head -5 >> ${{ env.REPORT_DIR }}/code-analysis.md
        else
          echo "✅ Табы не найдены" >> ${{ env.REPORT_DIR }}/code-analysis.md
        fi
        
        # 2. Статистика по файлам
        echo "" >> ${{ env.REPORT_DIR }}/code-analysis.md
        echo "## 2. Статистика файлов" >> ${{ env.REPORT_DIR }}/code-analysis.md
        
        # Подсчет Java файлов
        JAVA_COUNT=$(find . -name "*.java" | wc -l)
        echo "Всего Java файлов: $JAVA_COUNT" >> ${{ env.REPORT_DIR }}/code-analysis.md
        
        # Список самых больших файлов
        echo "" >> ${{ env.REPORT_DIR }}/code-analysis.md
        echo "Самые большие Java файлы:" >> ${{ env.REPORT_DIR }}/code-analysis.md
        find . -name "*.java" -exec wc -l {} \; 2>/dev/null | sort -rn | head -10 | while read line; do
          echo "- $line" >> ${{ env.REPORT_DIR }}/code-analysis.md
        done || echo "Не удалось получить список файлов" >> ${{ env.REPORT_DIR }}/code-analysis.md
        
        # 3. Поиск TODO, FIXME
        echo "" >> ${{ env.REPORT_DIR }}/code-analysis.md
        echo "## 3. TODO и FIXME комментарии" >> ${{ env.REPORT_DIR }}/code-analysis.md
        TODO_COUNT=$(grep -r "TODO\|FIXME" --include="*.java" . 2>/dev/null | wc -l || echo "0")
        if [ "$TODO_COUNT" -gt 0 ]; then
          echo "Найдено TODO/FIXME: $TODO_COUNT" >> ${{ env.REPORT_DIR }}/code-analysis.md
          grep -r "TODO\|FIXME" --include="*.java" . 2>/dev/null | head -5 >> ${{ env.REPORT_DIR }}/code-analysis.md
        else
          echo "✅ TODO/FIXME не найдены" >> ${{ env.REPORT_DIR }}/code-analysis.md
        fi
        
        echo "✅ Проверка качества кода завершена"
    
    - name: 📊 Generate summary report
      run: |
        echo "📊 Генерация итогового отчета..."
        mkdir -p ${{ env.REPORT_DIR }}
        
        echo "# 📊 Итоговый отчет CI/CD" > ${{ env.REPORT_DIR }}/summary.md
        echo "" >> ${{ env.REPORT_DIR }}/summary.md
        echo "## Информация о проекте" >> ${{ env.REPORT_DIR }}/summary.md
        echo "- Название: ${{ env.PROJECT_NAME }}" >> ${{ env.REPORT_DIR }}/summary.md
        echo "- Ветка: ${{ github.ref_name }}" >> ${{ env.REPORT_DIR }}/summary.md
        echo "- Дата сборки: $(date)" >> ${{ env.REPORT_DIR }}/summary.md
        echo "" >> ${{ env.REPORT_DIR }}/summary.md
        
        # Статистика проекта
        echo "## Статистика проекта" >> ${{ env.REPORT_DIR }}/summary.md
        if [ -f "${{ env.REPORT_DIR }}/code-analysis.md" ]; then
          grep -A5 "## 2. Статистика файлов" ${{ env.REPORT_DIR }}/code-analysis.md | tail -n +2 >> ${{ env.REPORT_DIR }}/summary.md
        fi
        
        echo "" >> ${{ env.REPORT_DIR }}/summary.md
        echo "## Результаты" >> ${{ env.REPORT_DIR }}/summary.md
        echo "- ✅ Сборка: Успешно" >> ${{ env.REPORT_DIR }}/summary.md
        echo "- ✅ Тестирование: Завершено" >> ${{ env.REPORT_DIR }}/summary.md
        echo "- ✅ Анализ кода: Завершен" >> ${{ env.REPORT_DIR }}/summary.md
        
        echo "✅ Отчет сгенерирован"
